import 'package:flutter/material.dart';

class StationMap extends StatelessWidget {
  const StationMap({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Bản đồ trạm xe sẽ hiển thị ở đây"));
  }
}
